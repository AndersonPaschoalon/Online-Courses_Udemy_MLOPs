* Rodando uma parte avulsa da pipeline
    python -m src.model_training.train_model


* Build da imagem docker
    docker build -t ml-classifier .
    docker run -p 5001:5001 ml-classifier

* Comandos úteis
    # lista processos docker
    docker ps 
    # lista containers docker
    docker container ls 
    # para um container 
    docker container stop 01f6 # inicio do id, que pode ser lido com comandos acima

* Instalar dependencias no projeto usando o pyproject.toml 
    pip install -e .
    


# dvc

* Rodar experimento modificando os parametros e saidas
dvc exp run -S train.learning_rate=0.1

* Rodar experimento sem modificar parametros
dvc exp run -S train.learning_rate=0.1 --temp

* Mostar experimentos
dvc exp show

* aplicar experimento 
dvc exp apply 5507256

# MLflow
mlflow ui

# Airflow
airflow webserver
airflow scheduler
